# Development Environment Configuration
spring.application.name=compliance-service

# PostgreSQL Database Configuration (Development)
spring.datasource.url=jdbc:postgresql://localhost:5432/rfid_compliance_db
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration (Development)
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# JWT Configuration (Development - Use a different secret in production!)
jwt.secret=dGhpcy1pcy1hLXZlcnktc2VjdXJlLXNlY3JldC1rZXktZm9yLWp3dC1hdXRoZW50aWNhdGlvbi1jaGFuZ2UtaW4tcHJvZHVjdGlvbg==
jwt.expiration=86400000

# Server Configuration
server.port=29289
server.servlet.context-path=/autocomply

# Logging Configuration (Development - More verbose)
logging.level.root=INFO
logging.level.com.example.compliance_service=INFO
logging.level.org.springframework.security=INFO
logging.level.org.hibernate.SQL=OFF
logging.level.org.hibernate.type.descriptor.sql=OFF
logging.level.org.hibernate.orm.jdbc.bind=OFF
logging.level.org.hibernate=WARN

# DevTools Configuration
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true

# ?? MQTT / HiveMQ Configuration ??????????????????????????????????????????????
# HiveMQ broker URL (tcp:// for plain, ssl:// for TLS)
mqtt.broker-url=ssl://p8446128.ala.asia-southeast1.emqxsl.com:8883
# Client ID must be unique per connection
mqtt.client-id=mqttx_c06420523
# Topic that the FX9600 reader publishes tag reads to
mqtt.topic=evnt/tag/fx9600/22361010554176
# QoS: 0=at-most-once, 1=at-least-once, 2=exactly-once
mqtt.qos=1
# Optional credentials (leave blank if broker has no auth)
mqtt.username=reader
mqtt.password=reader@2024
# Reader hardware details (used when not included in MQTT payload)
mqtt.reader-model=Zebra FX9600
# Reader IP is resolved dynamically from the MQTT payload; set a fallback here
mqtt.reader-ip-fallback=192.168.1.22

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=supunsandaruwan543@gmail.com
spring.mail.password=qfgk locl qbni uyuy
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
app.email.from=supunsandaruwan543@gmail.com
